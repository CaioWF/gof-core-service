# GoF Core Service

## Install

`npm install` and `sls dynamodb install` and `sls plugin install --name serverless-s3-local`

## Run Locally

`npm run dev`

## Deploy

`npm run deploy`

## Revert Deploy

`npm run remove`